export interface IUserButtonProps {
  /**
   * The button aria label.
   * @type {string}
   *
   * */
  ariaLabel: string;

  // as: ComponentWithAs<"button", MenuButtonProps>;

  // forwardRef: any;
}
